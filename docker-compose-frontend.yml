version: '3.8'

services:
  frontend:
    image: ${DOCKERHUB_USERNAME}/frontend-despachos:latest
    container_name: frontend_app
    ports:
      - "80:80"
    networks:
      - front-tier
    restart: always

networks:
  front-tier:
    driver: bridge